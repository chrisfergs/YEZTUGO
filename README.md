
  # Landing Page for AMLI

  This is a code bundle for Landing Page for AMLI. The original project is available at https://www.figma.com/design/zpR252RGauaKnikAA7Urt5/Landing-Page-for-AMLI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  